<IfModule mod_rewrite.c>
    Options -MultiViews
    RewriteEngine On

    # Redirigir todo el tráfico a index.php si el archivo o directorio no existe
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
